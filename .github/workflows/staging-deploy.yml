name: Legacy Easypanel Staging Fallback

on:
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Install dependencies
        run: npm install

      - name: Secret scan
        run: npm run secrets:check

      - name: Lint
        run: npm run lint

      - name: Build
        env:
          VITE_DASHBOARD_BASE: /
          DASHBOARD_APP_URL: /
        run: npm run build -- --mode staging

      - name: Security audit
        run: npm audit --audit-level=critical
        continue-on-error: false

      - name: Trigger legacy Easypanel staging webhook
        env:
          WEBHOOK_URL: ${{ secrets.EASYPANEL_STAGING_WEBHOOK }}
        run: |
          if [ -z "$WEBHOOK_URL" ]; then
            echo "Error: EASYPANEL_STAGING_WEBHOOK secret is empty or not set."
            exit 1
          fi
          echo "Triggering legacy Easypanel fallback for staging..."
          curl -X POST -f "$WEBHOOK_URL"
          echo "Legacy fallback triggered successfully."
